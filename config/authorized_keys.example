# authorized_keys.example —— 允許登入這個 dev container 的用戶端公鑰清單「範本」
#
# 使用方式：
#   1. 複製本檔為同目錄下的 authorized_keys（不含 .example）：
#        cp config/authorized_keys.example config/authorized_keys
#   2. 把你的公鑰加進去（.pub 內容，通常以 ssh-ed25519 或 ssh-rsa 開頭）：
#        cat ~/.ssh/id_ed25519.pub >> config/authorized_keys
#
# 說明：
#   - 一行一把公鑰；以 # 開頭的行會被 sshd 忽略（註解）。
#   - config/authorized_keys 會被 docker-compose.yml 掛載進容器，
#     並設為 SSH 使用者的 ~/.ssh/authorized_keys。
#   - 真實的 config/authorized_keys 已被 .gitignore 排除，不會進版控；
#     公鑰本身不是機密，但個人金鑰清單不入乾淨 main，避免混入個資。
#
# 格式範例（請換成你自己的公鑰，勿直接使用）：
# ssh-ed25519 AAAAC3NzaC1lZDI1NTE5AAAA...你的公鑰... you@your-mac
